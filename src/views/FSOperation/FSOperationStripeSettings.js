import React, {Component} from 'react';

export default class FSOperationStripeSettings extends Component {
    render (){
        return (
            <div>fs operation stripe settings</div>
        );
    }
}